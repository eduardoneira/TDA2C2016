package viajante.mst;

public class MSTGenerator {

	
	public void MST(){
		
	}

	
	
	/* Cormen
	MST-Prim(G, w, r):
		for each u in G.V
			u.key = infinite
			u.parent = NIL
		r.key = 0
		Q = G.V
		while !Q.isEmpty:
			u = Q.poll
			for each v in G.Adj(u):
				if v in Q and w(u,v) < v.key
					v.parent = u
					v.key = w(u,v)
	*/
}
